services:
  db:
    image: postgres:16
    container_name: wallets_transactions_db
    environment:
      POSTGRES_DB: wallets_transactions
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d wallets_transactions"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    container_name: wallets_transactions_app
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/wallets_transactions
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
